canvas node -> this will refare to another canvas, or open a file and import the data, it will only have an output node.
the input node will be just to lead file which is the json data format.

the canvas out put will ba named based on the name of the output from the imported node.

and you will add additinonal nodes to it 
canvase input node and canvas output node
canvas input -> this node will allow users to connect multiple input fields to it so that when the file is exported into the canvas node, the input fields will be used to populate the node's input data when users input data into the fields.
canvas output -> this node will allow users to connect the output of the canvas node to other nodes in the graph. listing all the available output fields from the canvas input node.

including custom data and nodes

eye drop color picker should be working fine and funtional

not every output should connect to every input.
note every input should connect to every output.
based on their data types.

every color recieving input should be able to take an image output node
as the color input, same as blender.

every node should be recalculated on node valu change.

for nodes location it ramains the same, every two seconds
when nodes are note moved it location should be saved.


for every output nodes that have a display, i need you to
make them bigger so their values can be seen properly as displayed.
add digital number counter 4 digits and should be expandable based on nodes settings
and it values should update based on input value change

for every nodes that write files, i need you to add additional button to them
they should not write files automaticaly, they should when the button is
clicked or trigard by another logic to click

and button clicked events should do same tooo.
